/**
 * @deprecated Import from `@rdlabo/workers-timezone` instead.
 *
 * This compatibility subpath re-exports the canonical workspace implementation so existing
 * consumers can migrate without a behavior change.
 *
 * @packageDocumentation
 */

export * from '@rdlabo/workers-timezone';
